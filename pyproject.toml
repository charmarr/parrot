[project]
name = "parrot"
version = "0.1.0"
description = "CI auto-healing for charmarr charms"
license = "GPL-3.0-only"
requires-python = ">=3.12,<3.14"
dependencies = [
    "theow[codegraph,claude-agent]>=0.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
