[project]
name = "parrot"
version = "0.1.0"
description = "CI auto-healing for charmarr charms"
license = "GPL-3.0-only"
requires-python = ">=3.12,<3.14"
dependencies = [
    "theow[codegraph]>=0.0.22",
]

[tool.uv]
package = false
